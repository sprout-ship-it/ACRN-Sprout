import React from 'react';
import ReactDOM from 'react-dom/client';
import RecoveryHousingApp from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <RecoveryHousingApp />
  </React.StrictMode>
);